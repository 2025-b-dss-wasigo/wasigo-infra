apiVersion: v1
kind: Service
metadata:
  name: wasigo-backend
  namespace: backend
spec:
  selector:
    app: wasigo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
