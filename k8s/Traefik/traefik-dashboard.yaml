apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`traefik.137.131.164.217.sslip.io`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
      # He comentado esto para que puedas entrar y validar el panel.
      # Luego lo activaremos quitando los símbolos '#'
      # middlewares:
      #   - name: dashboard-auth
  tls:
    secretName: traefik-dashboard-tls